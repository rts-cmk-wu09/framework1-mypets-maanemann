const HomeList = () => {
    return ( <p>content</p>
        // HomeHeader
            // ProfilePeak
                // ProfilePic
                    // PopBut (props)
                // CurrLocation
                // Bell
                    // PopBut (props)
            // PetTypes
                // CatsBut etc..
                    // PopBut (props)
        // PetList
            // GreyhCard
                // PetImg (props)
                // PetTitle (props)
                // PetLoca (props)
                // PetDescr (props)
                // HeartBut
                    // PopBut (props)
            // DoberCard
                // Etc...
            // Etc..
        // HomeFooter
            // HomeBut
                // PopBut (props)
            // MessBut
                // PopBut (props)
            // FaveBut
                // PopBut (props)
            // ProfBut
                // PopBut (props)
     );
}
 
export default HomeList;